apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - name: http
      port: 80         # 클러스터 내부에서 서비스가 받는 포트
      targetPort: 80   # 컨테이너 실제 리슨 포트 (기본 nginx는 80)
      nodePort: 30080  # 외부에서 접속할 포트(30000~32767 중 선택)
